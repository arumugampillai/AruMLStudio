# Operator Catalog

Closed pack **1.0.0** seed — see freeze table and `operators/operator_catalog.json`.

Regenerate artifacts:

```python
from feature_intelligence.operators import write_catalog_artifacts
write_catalog_artifacts()
```
